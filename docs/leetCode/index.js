/**
 * @param {string} senate
 * @return {string}
 */
var predictPartyVictory = function(senate) {
  let stack = [];
  let i = 0;
  while (i < senate.length) {
    if (senate[i] === "R") {
      senate.push(senate[i]);
    } else {
    }
  }
};
